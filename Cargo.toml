[package]
name = "blog_os"
version = "0.1.0"
edition = "2018"

[dependencies]

# Profile used for cargo build
[profile.dev]
panic = "abort"

# Profile used for cargo build --release ]
# DIsabling stack unwinding on panic
[profile.release]
panic = "abort"

[[bin]]
name = "blog_os"
test = false
bench = false